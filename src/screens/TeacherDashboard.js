import React from 'react';
import { View, Text } from 'react-native';

export default function TeacherDashboard() {
  //const profile = route.params;

  return (
    <View style={{ padding: 20 }}>
      <Text>This is teacher dashboard screen</Text>
    </View>
  );
}
